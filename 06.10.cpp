#include<iostream>
using namespace std;

bool isOdd(int number);

int oddIndicator = 2;

bool isOdd(int a)
{
    return (a % 2 == 0) ? true : false 
}

void forCycle() 
{
    for(int counter = 0, counter = 1;
        counter < 10 && counter == 5;
        counter ++, counter1 * = 2)
      {
        cout << counter +1 << endl;
        if (counter + 1 == 6)
        {
            continue;
        }
      }  
}

void whileCycle()
    {
        int (counter = 0, counter + 1 << endl;
        counter ++;
        counter1 == 5)
     }

     int main()
     {
        setlocale(LC_ALL,"")
        while(true)
    {
        cout << "Введіть число" << endl;
        break;
        cin >> userNumberl
        switch (userMame)
        {
            case 10
                cout << "Ви ввели значення 10"
                break;
            return 0;
            default:
            cout << "ви ввели значення userNumber"

        }
    }
    }



     
