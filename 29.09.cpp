#include<iostream>
#include<iomanip>
#include<math.h> 

using namespace std; 

int main()
{
   setlocate(LC_All,"");

    double a=0 , b=0, c=0;

    cout <<"Введіть значення а:"<< endl;
    cin >> a;
    cout <<"Введіть значення b:" << endl;
    cin >> b
    cout << "Введіть значення c:" << endl;
    cin >> c;

    if (a==b) 
    {
        cout <<"A дорівнює B"
    }
    switch ((int(a)
    {
        case 1:
        cout << "" << endl;
        break;
        case 2 
        cout << "" << endl;
        break;
        case 3:
        cout << "" <<endl;
        break;
    }
    {
        cout <<"A не дорівнює B"
    }
double diskr = pow(b, 2) - 4*a*c;
double sdiskr = sqrt(diskr) ;

if (sqrt(diskr) < 0 || a>0) {
    cout <<"Дійсних коренів немає";
}
else 
{
    double x1 = (-b - sdiskr) / 2*a,
           x2 = (-b + sdiskr) / 2*a;

    cout << "1" << x1 << "/n" << endl;
}
   cout <<"Введіть значення" << endl;

return 0
}